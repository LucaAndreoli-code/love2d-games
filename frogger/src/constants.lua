GAME_WIDTH = 13 * 16  -- 208px
GAME_HEIGHT = 12 * 16 -- 192px

GRID_WIDTH = 13       -- numero di colonne
GRID_HEIGHT = 12      -- numero di righe (corsie)
NUM_LILIPAD = 5       -- numero di ninfee nella corsia finale
HOP_DURATION = 0.5    -- durata del salto in secondi

return {
    GAME_WIDTH = GAME_WIDTH,
    GAME_HEIGHT = GAME_HEIGHT,
    GRID_WIDTH = GRID_WIDTH,
    GRID_HEIGHT = GRID_HEIGHT,
    NUM_LILIPAD = NUM_LILIPAD,
    HOP_DURATION = HOP_DURATION
}
